server.host = "0.0.0.0"
server.host = ${?SERVER_HOST}
server.port = 3003
server.port = ${?SERVER_PORT}

# Start potgres with the following:
# docker container run -d --name postgres-runner -p 5432:5432 -e POSTGRES_DB=mymoviedatabase -e POSTGRES_USER=busyUser -e POSTGRES_PASSWORD=qwerty postgres
psql.host = "localhost:5432"
psql.host = ${?PSQL_HOST}
psql.database = "mymoviedatabase"
psql.database = ${?PSQL_DATABASE}
psql.user = "busyUser"
psql.user = ${?PSQL_USER}
psql.password = "qwerty"
psql.password = ${?PSQL_PASSWORD}

